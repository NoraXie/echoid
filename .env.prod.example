# Server Configuration
ENV=prod
# MUST be your actual public HTTPS domain
HOST_URL=https://api.yourdomain.com
PROJECT_NAME="EchoID Server"
TZ=America/Mexico_City

# ECHOB (WhatsApp Service) - Production
ECHOB_API_URL=https://api.echob.com/v1
ECHOB_API_KEY=your_real_production_api_key

# AI Configuration (Optional)
# Required only if you run the template generation script
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxx

# Database (Production - Docker Service Name or Managed DB IP)
# If using docker-compose, host is usually 'db'
# Ensure POSTGRES_PASSWORD matches the password in DATABASE_URL
POSTGRES_PASSWORD=secure_password_change_me
DATABASE_URL=postgresql://postgres:secure_password_change_me@db:5432/echoid

# Redis (Production - Docker Service Name)
# If using docker-compose, host is usually 'redis'
REDIS_PASSWORD=secure_redis_password_change_me
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
