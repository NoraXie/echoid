# Server Configuration
ENV=prod
# 替换为您的公网 IP 或域名
HOST_URL=http://35.227.103.122
PROJECT_NAME="EchoID Server (Prod)"
TZ=America/Mexico_City

# ECHOB (WhatsApp Service) - Remote Configuration
# 替换为远端 Waha 服务器的地址
ECHOB_API_URL=http://<REMOTE_WAHA_IP>:3000
ECHOB_API_KEY=my_secure_api_key_123

# Database (Production)
POSTGRES_PASSWORD=secure_password
DATABASE_URL=postgresql://postgres:secure_password@db:5432/echoid

# Redis (Production)
REDIS_PASSWORD=secure_redis_password
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
